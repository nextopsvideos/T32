apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-dev-app.fullname" . }}
  labels:
    {{- include "my-dev-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-dev-app.selectorLabels" . | nindent 4 }}
